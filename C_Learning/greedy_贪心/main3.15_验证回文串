/*
3.15 验证回文串
给你一个字符串s，最多可以从中删除一个字符。请你判断s是否能成为回文字符串。
输入：s = "abca"
输出：1
输入：s = "abc"
输出：0

 *+
 */


#include <stdio.h>
#include "string.h"
#include <stdlib.h>

#define N 500
#define M 3

int judge(char back[], int backn){
    for (int i = 0; i < backn / 2; ++i) {
        if (back[i] != back[backn - 1 - i]) return 0;
    }
    return 1;
}

int output(char str[], int len){
    // 创建一个back[], 放去掉一个元素后的字符数组
    for (int i = 0; i < len; ++i) {
        char back[N] = {0};
        int backn = 0;
        for (int j = 0; j < len; ++j) {
            if (j != i) back[backn++] = str[j];
        }
        // 判断是否回文，是就直接返回
        if (judge(back, backn)) return 1;
    }
    return 0;
}

int main() {
    char str[N]={0};
    //s的长度
    int len = 0;
    gets(str);
    for (int i = 0; str[i]; ++i) {
        len++;
    }
    printf("%d\n", output(str, len));
    return 0;
}