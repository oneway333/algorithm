/*
 *
3.2 最优装载问题
已知有一艘船和n个集装箱，船的载重量为c，每个集装箱的重量使用wi来表示。求出最多有多少集装箱能装到船上？
输入：w = [2, 2, 6, 5, 4], c = 10
输出：3
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

#define N 50

void sort(int a[], int n){
    for (int i = 0; i < n - 1; ++i) {
        int k = i;
        for (int j = i + 1; j < n; ++j)
            if (a[k] > a[j]) k = j;
        if (k != i){
            int t = a[k];
            a[k] = a[i];
            a[i] = t;
        }
    }
}

int carry(int w[], int n, int c){
    int cap = 0, i = 0;
    // 升序排序
    sort(w, n);
    while (cap <= c){
        cap += w[i++];
    }
    return i - 1;
}

int main(){
    int w[] = {2, 2, 6, 5, 4}, n = 5, c = 10;
    printf("%d\n", carry(w, n, c));
    return 0;
}