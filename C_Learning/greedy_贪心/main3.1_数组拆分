/*
 *
3.1 数组拆分
给定长度为 2n 的整数数组 nums ，你的任务是将这些数分成 n 对, 例如 (a1, b1), (a2, b2), ..., (an, bn) ，使得从 1 到 n 的 min(ai, bi) 总和最大。返回该 最大总和 。
输入：nums = [1,4,3,2]
输出：4
输入：nums = [6,2,6,5,1,2]
输出：9
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

#define N 50

void sort(int a[], int n){
    for (int i = 0; i < n - 1; ++i) {
        int k = i;
        for (int j = i + 1; j < n; ++j) {
            if (a[k] > a[j]) k = j;
        }
        if (k != i){
            int t = a[k];
            a[k] = a[i];
            a[i] = t;
        }
    }
}

int max(int nums[], int n){
    int res = 0;
    // 升序排序
    sort(nums, n);
    for (int i = 0; i < n; i+=2) {
        res += nums[i];
    }
    return res;
}

int main(){
    int nums1[] = {1, 4, 3 ,2}, nums2[] = {6,2,6,5,1,2}, n1 = 4, n2 = 6;
    printf("%d\n", max(nums1, n1));
    printf("%d\n", max(nums2, n2));
    return 0;
}