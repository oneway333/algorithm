/*
 *
3.5 pow(a, n)
设计一个求次方的函数，要求能尽可能少的进行乘法计算。
输入：a = 2, n = 5
输出：32
输入：a = 3, n = 8
输出：6561
 */

#include <stdio.h>

int pow(int a, int n){
    if (n == 0) return 1;
    int t = a, k = 1;
    while (k * 2 <= n){
        k *= 2;
        t = t * t;
    }
    return t * pow(a, n - k);

}

int main(){
    int a, n;
    scanf("%d%d", &a, &n);
    printf("%d\n", pow(a, n));
    return 0;
}