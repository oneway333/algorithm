/*
 *
3.6 机器人寻路
一个机器人位于一个m x n网格的左上角，机器人每次只能向下或者向右移动一步。机器人试图达到网格的右下角，问总共有多少条不同的路径？
输入：m = 3, n = 7
输出：28
输入：m = 3, n = 2
输出：3
 */

#include <stdio.h>

#define N 50

int path(int m, int n){
    int dp[N][N] = {0};
    // 每次为走右边和走下边的dp的和，初始化最后一行，最后一列
    for(int i = 0; i < n; i++)
        dp[m - 1][i] = 1;
    for(int i = 0; i < m; i++)
        dp[i][n - 1] = 1;
    for (int i = m - 2; i >= 0; i--)
        for(int j = n - 2; j >= 0; j--)
            dp[i][j] = dp[i + 1][j] + dp[i][j + 1];
    return dp[0][0];
}

int main(){
    int m, n;
    scanf("%d%d", &m, &n);
    printf("%d\n", path(m, n));
    return 0;
}