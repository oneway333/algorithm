/*
 *
 *
 * 给定⼀个⻓度为n的整数数组 height ，有 n 条垂线，第 i 条线的两个端点是 （i，0） ，和
（i， height[i]） ，找出其中的两条线，使得他们与x轴共同构成的容器可以容纳最多的⽔。返回容器不倾斜的前提下可以储存的最⼤⽔量。
 * 输入：height = [1,8,6,2,5,4,8,3,7]
 *
 *
 *
 */

#include <stdio.h>

int main(){
    // 假设height[n]
    int height[] = {1, 8, 6, 2, 5, 4, 8, 3, 7}, n = 9, max = 0;
    for (int i = 0; i < n - 1; i++)
        for (int j = i + 1; j < n; j++){
            int t = (height[i] < height[j] ? height[i] : height[j]) * (j - i);
            max = max > t ? max : t;
        }
    printf("max is %d", max);
    return 0;
}