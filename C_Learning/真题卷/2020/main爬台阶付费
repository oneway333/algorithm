/*
 *
 *
 * 给你⼀个整数数组 cost ，其中 cost[i] 是从楼梯第i个台阶向上爬所需要⽀付的费⽤，⼀旦你⽀
付此费⽤，就可以选择向上爬⼀个或者两个台阶，你可以选择从新下标为0或1的台阶开始爬楼梯，请你计算并返回达到楼梯顶部的最低花费
 *
 *
 *
 *
 */

#include <stdio.h>

#define N 50

int main(){
    // 假设cost[n]，认为n - 1阶为顶部，花费为0
    int cost[] = {2, 1, 6, 9, 5, 5, 0}, n = 7;
    // dp[i]表示从第i阶台阶到达顶部的费用
    int dp[N] = {0}, i;
    dp[n - 2] = cost[n - 2];
    for (i = n - 3; i >= 0; i--)
        dp[i] = (dp[i + 1] < dp[i + 2] ? dp[i + 1] : dp[i + 2]) + cost[i];
    printf("min cost is %d\n", dp[0] < dp[1] ? dp[0] : dp[1]);
    return 0;
}