/*
 *
 *
 * 4位数是完全平方，前两位相同，后两位相同
 *
 *
 *
 */

#include <stdio.h>

int main(){
    int n;
    for (int i = 1; i <= 100; i++){
        n = i * i; // n一定为一个完全平方数
        if (n < 1000) continue; // 确保n为4位数
        if (n > 9999) break;
        int a = n / 1000;
        int b = n / 100 % 10;
        int c = n / 10 % 10;
        int d = n % 10;
        if (a == b && c == d)
            printf("%d\n", n);
    }
    return 0;
}