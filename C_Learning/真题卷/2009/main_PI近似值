/*
 *
 Π近似值
 */

#include <stdio.h>
#include <math.h>

int main(){
    int i = 1, flag = 1;
    double pi = 0, t = 1;
    while (fabs(t) > 1e-6){
        t = 1.0 * flag / (2 * (i++) - 1);
        pi += t;
        flag = -flag;
    }
    printf("%lf\n", 4 * pi);
    return 0;
}
