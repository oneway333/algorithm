/*
 *
 *
 * x的n次幂
 *
 *
 *
 *
 */

#include <stdio.h>

double pow(double x, int n){
    if (n == 0) return 1;
    if (n == 1) return x;
    return x * pow(x, n - 1);
}

int main(){
    double x;
    int n;
    scanf("%lf %d", &x, &n);
    printf("%lf\n", pow(x, n));
    return 0;
}