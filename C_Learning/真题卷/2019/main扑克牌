/*
 *
 *
 * 52张扑克牌
 *
 *
 *
 *
 */

#include <stdio.h>

int main(){
    // 0表示正面向上，1表示向下
    int a[53] = {0}, n = 52;
    for (int i = 2; i <= n; ++i)
        for (int j = i; j <= n; j++)
            if (j % i == 0) a[j] = 1 - a[j]; // 1- 表示在0和1之间徘徊
    int s = 0;
    for (int i = 1; i <= n; i++)
        if (a[i] == 0) s++;
    printf("%d up\n", s);
    return 0;
}