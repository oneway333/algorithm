/*
 *
1， 2， 3， 4候选人
 最后输出票数超过一半的
 -1为退出
 */

#include <stdio.h>

int main(){
    int cands[4] = {0}, n = 0, cand;
    while (1){
        scanf("%d", &cand);
        if (cand == -1) break;
        if (cand == 1) cands[0]++;
        if (cand == 2) cands[1]++;
        if (cand == 3) cands[2]++;
        if (cand == 4) cands[3]++;
        n++;
    }
    for (int i = 0; i < 4; i++)
        if (cands[i] * 1.0 / n > 0.5)
            printf("%d\n", i + 1);
    return 0;
}