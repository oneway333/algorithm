#include <stdio.h>
#include <stdlib.h>
#include "math.h"

void count(int m, int n){
    int a, b;
    b = (n - 2 * m) / 2;
    a = m - b;
    printf("%d %d", a, b);
}

int main(){
    int m, n;
    scanf("%d%d", &m, &n);
    count(m, n);
    return 0;
}
