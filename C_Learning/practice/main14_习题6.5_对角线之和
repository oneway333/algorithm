#include<stdio.h>
#include<string.h>

#define N 40

int main(){
    int a[][3] = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    int s = 0;
    for (int i = 0; i < 3; ++i) {
        s += a[i][i];
    }
    printf("%d", s);
    return 0;
}