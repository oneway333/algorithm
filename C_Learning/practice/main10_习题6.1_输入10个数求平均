#include<stdio.h>
#include<string.h>

#define N 500

int main(){
    int s = 0;
    for (int i = 0; i < 10; ++i) {
        int j = 0;
        scanf("%d", &j);
        s += j;
    }
    printf("%lf", 1.0 * s / 10);
    return 0;
}