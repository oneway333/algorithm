#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define N 50

void sort(char str [][N], int n){ // 降序
    for (int i = 0; i < n - 1; ++i) {
        int k = i;
        for (int j = i + 1; j < n; ++j) {
            if (strcmp(str[k], str[j]) < 0) k = j;
        }
        if (k != i){
            char t[N] = {0};
            strcpy(t, str[k]);
            strcpy(str[k], str[i]);
            strcpy(str[i], t);
        }
    }
}

void write(char str[][N], int n){
    FILE *fp;
    if ((fp = fopen("data.txt", "w")) == NULL){
        printf("file open failed");
        exit(0);
    }
    for (int i = 0; i < n; ++i)
        fprintf(fp, "%s\n", str[i]);
    fclose(fp);
}

int main(){
    int n = 0;
    char str[][N] = {0};
    scanf("%d", &n);
    for (int i = 0; i < n; ++i)
        scanf("%s", str[i]);
    sort(str, n);
    write(str, n);
    return 0;
}
