/*
 *
3.6 双和数组
把给定偶数2n分解为6个互不相等的正整数a, b, c, d, e, f，然后把这6个数分成(a, b, c)与(d, e, f)两个3元组，若这两个组数具有以下双和相等特性：
a+b+c=d+e+f
1/a+1/b+1/c=1/d+1/e+1/f
则把3元组(a, b, c)与(d, e, f)（约定a < b < c，d < e < f,a < d）称为基于n的双和数组。输入一个整数n，输出双和数组的个数。
输入：n = 180
输出：4
输入：n = 200
输出：4
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int arrs(int n){
    int res = 0;
    int a, b, c, d, e, f;
    // a + b + c = n, a < b < c 推得上下界
    for (a = 1; a <= n - 2; ++a)
        for(b = a + 1; b <= n - 1; b++)
            for (d = a + 1; d <= n - 2; d++)
                for(e = d + 1;e <= n - 1; e++){
                    c = n - a - b;
                    f = n - d - e;
                    if (c < a || c < b || f < d || f < e) continue;
                    if (d * e * f * (b * c + a * c + a * b) == a * b * c * (e * f + d * f + d * e)) res++;
                }
    return res;
}

int main(){
    int n;
    scanf("%d", &n);
    printf("%d", arrs(n));
    return 0;
}
