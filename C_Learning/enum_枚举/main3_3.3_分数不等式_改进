#include <stdio.h>
#include <math.h>

int main(){
    double s = 0;
    int m1, m2, n = 1, flag = 0;
    scanf("%d %d", &m1, &m2);
    while (1){
        s += sqrt(n) / (n + 1);
        if (flag == 0 && s > m1){
            printf("[%d,", n);
            flag = 1;
        }
        if (s >= m2){
            printf("%d]", --n);
            break;
        }
        n++;
    }
    return 0;
}