/*
 *

3.3 佩尔方程
佩尔方程是关于x, y的二次不定方程x^2 - ny^2 = 1（其中n为非平方正整数）。当x = ±1, y = 0时，显然满足方程。常把x, y中有一个为零的解称为平凡解。
 输入n求佩尔方程的非平凡解(x > 0, 0 < y ≤ 10000)的个数。

 输入：n = 7
输出：3
输入：n = 12
输出：4

 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int form(int n){
    int res = 0;
    for (int i = 1; i <= 10000; ++i) {
        int x2 = n * i * i + 1;
        int x = sqrt(n * i * i + 1);
        if (x2 == x * x) res++;
    }
    return res;
}

int main(){
    int n;
    scanf("%d", &n);
    printf("%d", form(n));
}