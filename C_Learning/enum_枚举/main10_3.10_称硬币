/*
 *
3.10 称硬币
有12枚硬币（用A-L标识），其中有11枚真币和1枚假币。假币和真币重量不同，但不知道谁轻谁重。现在用一架天平称了这些币三次，告诉你称的结果，请你找出假币（数据保证一能找出来）。
测试数据分三行，分别表示每一次称量结果。每一行由三个字符串组成，前两个表示天平两段的硬币情况，最后一段表示天平右侧的状态：even - 平、up - 高、down - 低。
输入：
ABCD EFGH even
ABCI EFJK up
ABIJ EFGH even
输出：K
输入：
ABFK CEGH down
AGHC KFLD even
ADCE IFJK up
输出：E
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

#define N 50

void fake(){
    int coin[12] = {0};
    char left[N] = {0}, right[N] = {0}, bal[N] = {0};
    for (int i = 0; i < 3; ++i) {
        scanf("%s %s %s", left, right, bal);
        strcat(left, right);// strcpy是全量替换，strcat才是拼接。。。。果然应该debug。。。。
        int even = strcmp(bal, "even") == 0 ? -1 : 1;
        for (int j = 0; left[j] ; ++j) {
            coin[left[j] - 'A'] += even;
        }
    }
    int i, j;
    for (i = 0, j = 0; i < 12; ++i) {
        if (coin[i] > coin[j]) j = i;
    }
    printf("%c\n", j + 'A');
}

int main(){
    fake();
    return 0;
}