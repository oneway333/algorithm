/*
 *
 * 3.1 全素组
如果不大于指定整数n的3个互不相同的素数之和仍为素数，则把这3个素数称为一个基于n的全素组。例如对于n = 15, 素数3, 5, 11之和3 + 5 + 11 = 19为素数，
 则3, 5, 11称为一个基于15的全素组。 输入整数n (n ≤ 3000)，输出基于n的全素组的个数。
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

int isPrime(int num){
    int s = sqrt(num), i;
    for (i = 2; i <=s ; ++i) {
        if (num % i == 0) return 0;
    }
    if (i > s) return 1;
}

int calculate(int n){
    int a, b, c, res = 0;
    for (a = 2; a <= n - 2; ++a) {
        for (b = a + 1; b <= n - 1 ; ++b) {
            for (c = b + 1; c <= n ; ++c) {
                if (isPrime(a) && isPrime(b) && isPrime(c) && isPrime(a + b + c)) res++;
            }
        }
    }
    return res;

}

int main(){
    int n;
    scanf("%d", &n);
    printf("%d", calculate(n));
}

