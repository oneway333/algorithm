/*
 *
3.5 因数比
设整数a (a > 1)的小于其本身的因数之和为s，定义p(a) = s / a为整数a的因数比。试求指定区间[x, y]中整数的因数比最大值。
输入：x = 2, y = 5
输出：0.75
输入：x = 3, y = 6
输出：1
 */

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

double ratio(int x, int y){
    double max = 0.0;
    for (int a = x; a <= y; ++a) {
        double s = 0.0;
        for (int i = 1; i < a; ++i) {
            if (a % i == 0) s += i;
        }
        double p = s / a;
        max = max > p ? max : p;
    }
    return max;
}

int main(){
    int x, y;
    scanf("%d%d", &x, &y);
    printf("%lf", ratio(x, y));
}
